{
	"buildCommand": "bun install && (cd packages/core && bun run build) && (cd packages/theme-default && bun run build) && (cd packages/cli && bun run build) && (cd docs && bun run build.ts)",
	"outputDirectory": "docs/dist",
	"installCommand": "bun install",
	"framework": null
}
