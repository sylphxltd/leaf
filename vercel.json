{
	"buildCommand": "bun install && bun run build && cd docs && bun run build",
	"outputDirectory": "docs/dist",
	"installCommand": "bun install",
	"framework": null,
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/index.html"
		}
	]
}
